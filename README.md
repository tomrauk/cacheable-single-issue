### Project to exhibit issue with cacheable and reactive types.

The same request is made with and without `@Cacheable` annotation for a return type of `Single`. The cacheable method eventually timesouts.
